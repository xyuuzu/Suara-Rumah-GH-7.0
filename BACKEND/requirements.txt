# --- Backend API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9      # buat handle upload file audio
pydantic==2.9.2

# --- Audio processing ---
# numpy/scipy versi bebas (bukan pin ketat) karena versi lama (numpy 1.26.4,
# scipy 1.13.1) belum ada wheel prebuilt buat Python 3.13+ dan bakal gagal
# build dari source (butuh compiler Fortran). Biarkan pip pilih versi terbaru
# yang kompatibel dengan Python di environment ini.
librosa>=0.10.2
soundfile==0.12.1
numpy>=1.26.4
scipy>=1.13.1

# --- Machine Learning ---
scikit-learn>=1.5.1          # sama alasannya, biar dapet wheel bukan build dari source
joblib==1.4.2                # buat save/load model .pkl

# --- Text processing (deteksi dari teks) ---
Sastrawi==1.0.1              # stopword remover & stemmer Bahasa Indonesia

# --- Redis (caching/filtering hasil ekstraksi fitur suara & teks) ---
redis==5.0.8

# --- Firebase ---
firebase-admin==6.5.0

# --- Twilio (WhatsApp/SMS alert) ---
twilio>=9.0.0

# --- Utility ---
python-dotenv==1.0.1
pandas>=2.2.2
