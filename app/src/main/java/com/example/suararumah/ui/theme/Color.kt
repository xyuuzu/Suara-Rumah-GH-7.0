package com.example.suararumah.ui.theme

import androidx.compose.ui.graphics.Color

// ── Suara Rumah Color Palette (dari GuideStyle.md) ──

// Primary — Hijau Kalem: brand utama, tombol "Aman", toggle aktif, ikon utama
val Primary = Color(0xFF2A9D8F)
val PrimaryDark = Color(0xFF21867A)
val PrimaryLight = Color(0xFF4DB8AB)

// Background — Off-White: latar seluruh layar
val Background = Color(0xFFF8F9FA)

// Surface — Putih Bersih: latar card/widget/wadah grafik
val Surface = Color(0xFFFFFFFF)

// Text Primary — Dark Slate: judul, body text
val TextPrimary = Color(0xFF2B2D42)

// Text Secondary — Muted Grey: caption, label, status "Tidak memantau"
val TextSecondary = Color(0xFF8D99AE)

// Alert/Danger — Muted Coral: garis grafik anomali, state tombol grace period
val AlertDanger = Color(0xFFE76F51)

// Alert/Warning — Amber/Orange
val AlertWarning = Color(0xFFF4A261)

// ── Dark Mode Colors (nice-to-have) ──
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkTextPrimary = Color(0xFFE0E0E0)
val DarkTextSecondary = Color(0xFF9E9E9E)